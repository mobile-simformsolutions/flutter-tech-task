import 'package:flutter/material.dart';

/// Holds all the colors used in the app
class AppColors {
  /// refers to the background color of the app
  static const Color appBackground = Color(0xFFEBEEF9);

  /// Text color to be used in the app
  static const Color primaryText = Color(0xFF5D6579);

  /// accent color of the app
  static const Color accentColor = Color(0xFF2850FF);
}
